# P9c Sharing UI + read-only mode (~35-40 min, mid-tier OK). Read docs/ARCH-AUTH.md "HTTP API", "Client"; docs/DESIGN.md "Components", "UX rules". Client only.
## Link sharing (scope: link sharing only, no people list this pass)
"Share" button opens a popover anchored to it (not a modal): closes on Esc/outside click, the one allowed shadow. Section "General access": segmented control Restricted | Anyone with the link; when public, a role select (Viewer | Editor) with a one-line explanation ("Anyone with the link can view/edit, no sign-in needed"). "Copy link" button (copy in the click handler; toast "Link copied"). Owner sees controls (PUT share on change, optimistic with rollback on error toast); non-owners see the current setting read-only. Title edits persist via PATCH (owner/editor). Viewer role: banner "View only", no editor, Delete/paste ignored, formula bar read-only; ROLE messages switch modes live; ERROR forbidden -> "Access removed" panel.
Tests: share reducer/validation; role -> UI capability mapping; ROLE message handling.
Verify (manual, report): with two Google accounts (or dev sign-ins) in two windows: public editor link works as guest in a third window; switch to Restricted and the guest is removed live; live editing between owner and editor still syncs, presence shows Google names. npm test, typecheck, build, npm run sim. Commit, tag p9c-green, reply in 5 lines or fewer and stop.
