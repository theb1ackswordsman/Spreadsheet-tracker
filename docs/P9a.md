# P9a Server auth + per-sheet storage + ACL (STRONGEST model, ~75 min). Read docs/ARCH-AUTH.md fully, docs/ARCH.md "Server" and "Protocol". No UI changes.
Steps (one commit each, tag at the end): (1) protocol changes per ARCH-AUTH "Protocol changes" + ARCH.md update; `npm i google-auth-library` (only new dep). (2) `src/server/auth.ts`: sessions (hashed sid, persisted), cookie helpers, code exchange + verifyIdToken behind injectable `verifyGoogleCode`, dev login, rate limits, Origin/X-Requested-With checks. (3) `src/server/sheets.ts` (storage, roleFor, index, REST handlers), wire routes in index.ts (`startServer` opts: `{ port, persist, auth: 'on' | 'off', verifyGoogleCode, dataDir }`; index.ts always uses auth 'on'). (4) WebSocket: cookie identity, JOIN role gate, viewer EDIT rejection + SNAPSHOT resend, share-change propagation, room eviction. (5) Tests.
Tests (vitest, no network, temp dataDir) — one case per line, no need to split further: roleFor matrix (owner/acl/public/guest/none, highest wins); restricted JOIN as guest -> signin_required+close, as signed-in stranger -> forbidden; viewer EDIT rejected (no version bump, fresh SNAPSHOT follows), editor EDIT works and reaches viewer; public sheet as guest (editor + viewer); revoking access closes a live socket; role change sends ROLE then old permissions no longer apply; cookie flags (HttpOnly/SameSite=Lax/Secure under prod); cross-origin POST and missing X-Requested-With rejected; email_verified=false rejected; acl size/email validation; session survives restart; two sheets stay isolated; sim (`npm run sim`, auth 'off') still converges.
Verify: npm test, npm run typecheck, npm run build, npm run sim. Update STATUS.md, commit, tag p9a-green, reply in 5 lines or fewer and stop.
