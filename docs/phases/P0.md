# P0 Scaffold (30 min, cheaper model OK)
Create, with NO app logic:
- `package.json` (npm). Scripts: dev=`concurrently "tsx watch src/server/index.ts" "vite"`, test=`vitest run --reporter=dot`, typecheck=`tsc --noEmit`, build=`vite build`, start=`tsx src/server/index.ts`, sim=`tsx sim/run.ts`.
- `tsconfig.json` (strict, jsx react-jsx, moduleResolution bundler, include src + sim), `vite.config.ts` (react plugin; proxy `/ws` -> `ws://localhost:8787` with `ws: true`), `index.html`, `.gitignore` (node_modules, dist, data).
- `src/engine/types.ts` and `src/shared/protocol.ts`: verbatim from ARCH "Types" and "Protocol". `src/engine/constants.ts` from ARCH "Engine".
- `src/client/styles/tokens.css` from DESIGN "Tokens"; `src/client/main.tsx`, `App.tsx`: empty shell using tokens.
- `src/server/index.ts`: http + WebSocketServer on :8787 path `/ws`, log connect/disconnect.
- `src/engine/sanity.test.ts`: one passing test.
Then `git init`.
Verify: `npm run typecheck` clean, `npm test` green, `npm run dev` starts both; in the browser console `new WebSocket('ws://'+location.host+'/ws')` logs a connection on the server.
Tag: p0-green.
