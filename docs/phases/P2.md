# P2 Client single-player (90 min). Read ARCH "Client", DESIGN "Tokens", "Layout" items 3-4, "Components". No server yet.
Traps: (1) `useSyncExternalStore` getSnapshot must return a referentially stable value; store immutable Results, replace only on change. (2) Worker/store/bridge are module singletons. (3) Worker: `new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' })`. (4) Display numbers via `Number(v.toPrecision(12))`. (5) Batch all PATCH cells per animation frame into one store notification. (6) Handlers use store/bridge methods, never React state (stale closures).

## P2a worker + bridge + store (no UI)
Files: `src/client/worker.ts` (owns Engine; handles INIT/APPLY, replies PATCH; try/catch around everything; coalesce messages per tick), `bridge.ts` (post + onmessage -> store), `store.ts`. Verify via a temporary console hook: apply edits, log values.

## P2b grid
Files: `Grid.tsx`, `Cell.tsx` (memo), `useCell.ts`, `nav.ts` (state machine). Virtualized rows (fixed 28px, spacer + translateY container, overscan 4, rAF scroll), sticky headers, active header highlight, selection, arrow/Tab/Enter movement. Grid container `tabIndex=0` handles keys in navigate mode only. Dev-only render counter (module var, exposed to store meta).

## P2c editing + formula bar
Files: `Editor.tsx`, `FormulaBar.tsx`. Edit mode mounts input with autofocus, seeded with `raw` (NOT the displayed value). Typing starts edit; F2 edits; Enter/Tab/blur commit; Esc cancels; Delete clears; after commit refocus grid. Remote updates never overwrite the active editor buffer. Commit = optimistic bridge.apply + (later) socket EDIT. Name box shows active cell. Formula bar edits `raw`.
Verify: `=SUM(A1:A5)`, edit A3 -> SUM updates; typing `=A1+` shows #VALUE! without console errors; dev render counter only counts visible cells.
Tag: p2a-green, p2b-green, p2c-green.
