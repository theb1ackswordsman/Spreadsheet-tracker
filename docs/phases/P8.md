# P8 QA + rehearsal (75 min, human-led; AI fixes only demo-path bugs)
1. Prod run: server serves `dist/` (static, path-traversal safe) when `NODE_ENV=production`; add `npm run start:prod` = `vite build && NODE_ENV=production tsx src/server/index.ts`. Demo ONLY on this build.
2. Manual QA across 3 windows: empty/never-set cell in formula = 0; edit inside SUM range updates it; clear cell; #DIV/0!; malformed formula; direct/indirect/self cycle then fix; same-cell collision + toast; drop/restore with queued edit; hostile inputs; no console errors for the full run.
3. Visual QA: DESIGN checklist (no gradients/glows/emojis, tokens only, 1366x768 and 1920x1080, formula cell edits show `=...` in editor and bar, numbers right-aligned without jitter).
4. Demo (5 beats): (1) 3 windows, shared SUM, presence borders, Performance strip visible. (2) Stress data, type in A1, toggle Naive vs Incremental. (3) Same-cell collision toast, then a joint cycle -> #CIRCULAR! everywhere -> fix recovers. (4) `?debug` drop connection mid-edit, restore, replay. (5) `npm run sim` prints converged.
   Line: "The server only orders edits. Each client derives the same values in a worker from the same ordered inputs, and only the affected subgraph recomputes."
5. Rehearse 3 times; record a backup video.
