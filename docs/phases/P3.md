# P3 Server + sync (60 min). Read ARCH "Server", "Consistency", "Client". 
Traps: WebSocket URL derived from `location` (`ws(s)://${location.host}/ws`); handlers call singletons, not React state; applying an OP whose raw already matches is a no-op.
Do:
1. `src/server/`: `room.ts`, `index.ts` per ARCH "Server" (JOIN/SNAPSHOT, EDIT validate + version + log + OP broadcast, RESUME). No formula parsing. Add unit tests for validation (bad cell ids, oversize raw, opId replay).
2. `src/client/socket.ts` singleton: connect, JOIN, auto-reconnect (backoff 250ms..5s), on SNAPSHOT -> `bridge.init`, on OP/OPS -> `bridge.apply` in order (own echo included), commit path sends EDIT with monotonic opId and tracks `pending`.
Verify: two tabs stay in sync; refresh restores from SNAPSHOT; log shows monotonic versions.
Tag: p3-green.
