# P4 Presence + resilience (60 min, STRONGEST model). Read ARCH "Server" (presence, RESUME), "Consistency", "Client"; DESIGN "Components".
Do:
1. Presence: SELECT on selection change (client), throttled PRESENCE broadcast (server), ping/pong cull, avatars in top bar, remote selection border + name tag in grid.
2. RESUME: client sends `lastVersion`; server replies OPS or SNAPSHOT; client then resends unacked `pending` edits with their original opIds (server dedupes by `opId`).
3. UI: reconnecting banner with queued count; grid stays editable offline.
4. Toast component (store meta) + overwrite toast per ARCH "Consistency".
5. `?debug` query param shows a "Drop connection" button that closes the socket without disabling auto-reconnect.
Verify: 3 tabs show 3 users; drop, type offline, restore -> edit replays, all tabs match; same-cell collision shows the toast on the loser.
Tag: p4-green.
