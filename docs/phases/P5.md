# P5 UX polish (60 min, cheaper model OK). Read DESIGN fully. NO logic changes except where noted.
Apply DESIGN layout items 1,2,3,4,6 and Components exactly. Checklist:
- Top bar (title editable locally, connection text, avatars, Share copies `location.href`), toolbar text buttons, status bar.
- Error explanation line under formula bar for the selected error cell.
- Sample data: `src/client/sample.ts` small budget sheet with `=SUM`; auto-send once as an EDIT batch when SNAPSHOT is empty; `Sample data` button re-sends it. Dismissible hint bar (remember dismissal in memory only).
- Shortcuts popover on `?` and toolbar button.
- Clipboard: `copy`/`paste` DOM events on the grid container; copy = active cell raw; paste TSV -> one EDIT batch from the active cell (clip to grid bounds).
- a11y: `role="grid"`, aria indexes, focus rings.
Verify at 1366x768 and 1920x1080: no page scroll, no gradients/glows/emojis, `grep -rE "#[0-9A-Fa-f]{3,6}|rgb" src/client --include=*.tsx --include=*.css` finds colors only in tokens.css.
Tag: p5-green.
