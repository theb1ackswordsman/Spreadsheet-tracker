# P7 Performance panel + stress (45 min, cheaper model OK). Read DESIGN "Layout" item 5.
Do:
1. Performance strip: scope / populated / ms / mode / cells re-rendered last update / version / RTT (ping every 5 s). Segmented control sets the mode sent in APPLY (`inc` | `naive`) locally only.
2. `Stress test` button (`src/client/stress.ts`): A1=1; B1:K1000 = `=$A$1*<col index>` (10,000 formulas); M1==`=A1`, M2..M1000 = `=M<prev>+1` (1,000-deep chain). Send in EDIT batches of <=2000 edits (server limit 5000/msg, rate limit 30 msg/s).
3. Tune if needed: memoized Cell, rAF batching, avoid full-map copies.
Verify: editing A1 on stress data stays smooth in Incremental; Naive is visibly slower; panel numbers are legible and tabular.
Tag: p7-green. FEATURE FREEZE after this phase.
