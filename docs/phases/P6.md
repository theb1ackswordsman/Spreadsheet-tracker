# P6 Hardening + bot sim (60 min). Read ARCH "Server", "Consistency".
Do:
1. Server: enforce all ARCH "Server" limits (maxPayload, rate limit, origin check, sanitize names), persistence to `data/sheet.json`, `GET /debug/hash` returning FNV-1a of sorted `id=raw`.
2. Client worker watchdog: if no PATCH within 2 s of an APPLY, terminate and respawn worker, re-INIT from `store.raw`.
3. `sim/run.ts` (args: bots=20 edits=2000): each bot = real WebSocket client + its own Engine instance (no worker). Random ops from A1:H20: numbers, formulas over random cells/ranges, ~5% deliberate cycles, ~5% clears, ~2% random disconnect/reconnect (RESUME). After all edits, wait until every bot reaches the server version; each bot computes FNV-1a over sorted `id=raw` and `id=value|error`. Print `converged` if all bot hashes match each other AND the raw hash matches `/debug/hash`; otherwise print the first differing cell and exit 1.
Hostile-input tests: `__proto__`, `A99999`, `SUM(A1:ZZ99999)`, 100-deep parens, 10 MB message -> server and worker survive.
Verify: `npm run sim` prints converged 3 runs in a row.
Tag: p6-green.
