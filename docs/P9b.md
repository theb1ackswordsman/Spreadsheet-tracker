# P9b Landing + Google sign-in + routing (~60 min, mid-tier OK). Read docs/ARCH-AUTH.md "Auth flow", "Client"; docs/DESIGN.md "Tokens", "Direction". Don't touch src/engine or src/server; if a server change seems needed, stop and ask.
Build: `router.ts` (History API), `auth.ts` (client: /api/config, /api/me, GIS script loader, code client, sign-in, logout), `Landing.tsx`, `AuthPanel.tsx`, `SheetPage.tsx` (existing app moved here, receives sheetId), route boot per ARCH-AUTH "Client", `resetForSheet(id)` for store/bridge/socket, socket JOIN uses route's sheetId. Vite proxy: add `/api` -> http://localhost:8787. Top-bar account menu (initials avatar, name, email, "New sheet", up to 10 recent sheets, "Sign out"); text items only.

Landing (YC-style, minimal, white, 1 column, max-width 720px, generous whitespace): Nav — wordmark "Tessera" left, "Sign in" text link right. Hero — H1 "Spreadsheets that always agree." (48-56px, weight 600, tight tracking, --text), one --muted sentence under it: "Real-time collaboration on a formula engine that recalculates only what changed." Primary button "Start working" (--accent bg, white text, 4px radius, 40px tall) calling `requestCode()` in its click handler. Below: three short text blocks in a row (stack under 720px) — "Recalculates only what changed" / "Every collaborator converges to the same state" / "Share by link, control who can edit". Footer: one muted line. If GET /api/config says devLogin true, show a small "Dev sign-in" text link. Banned: gradients, glows, emojis, illustrations, stock imagery, fake logos/testimonials, animated backgrounds. Popup-blocked/closed errors: plain inline message under the button.
Tests: router parse/serialize; `resetForSheet` clears cells and pending; sign-in error mapping.
Verify (manual, report): sign-in (Google or dev) lands in a sheet at /s/<id>; refresh keeps session; deep link while signed out shows AuthPanel and returns to it after sign-in; sign out returns to landing; New sheet gives an empty isolated sheet. npm test, typecheck, build. Commit, tag p9b-green, reply in 5 lines or fewer and stop.
