import React from 'react';

export function App() {
  return (
    <div
      style={{
        background: 'var(--bg)',
        color: 'var(--text)',
        fontFamily: 'var(--font-ui)',
        minHeight: '100vh',
        padding: 'var(--space-4)',
      }}
    >
      <h1>Collaborative Spreadsheet</h1>
    </div>
  );
}
